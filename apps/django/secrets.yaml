# Django — SECRET_KEY, database credentials, third-party API keys

- kind: allow
  values:
    - "django"
    - "django.contrib"
    - "djangoproject.com"
    - "localhost:8000"
    - "127.0.0.1:8000"
