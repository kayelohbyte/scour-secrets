# Spring Boot — application.yml, application.properties, datasource credentials

- kind: allow
  values:
    - "spring"
    - "spring.io"
    - "spring-boot"
    - "springframework.org"
    - "localhost:8080"
    - "127.0.0.1:8080"
    - "actuator"
