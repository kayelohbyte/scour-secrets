# Ansible — vault passwords, SSH keys, inventory credentials

- kind: allow
  values:
    - "ansible"
    - "ansible.com"
    - "galaxy.ansible.com"
    - "ansible-galaxy"
    - "ansible-playbook"
    - "ansible-vault"
